
pool:
  vmImage: 'windows-latest'

variables:
  projectPath: src\Socially.MobileApp


steps:

  - script: dotnet publish -c release -f net7.0-android -o published
    displayName: Compile MAUI android
    workingDirectory: $(projectPath)

  - publish: published