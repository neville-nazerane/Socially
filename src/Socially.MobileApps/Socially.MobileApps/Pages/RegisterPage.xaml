<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Socially.MobileApps.ViewModels;assembly=Socially.MobileApps.ViewModels"
             x:Class="Socially.MobileApps.Pages.RegisterPage"
             x:DataType="vm:RegisterViewModel">
        
    <ContentPage.Content>
        <StackLayout>

            <Entry Text="{Binding Model.Email}" />
            <Entry Text="{Binding Model.UserName}" />
            <Entry Text="{Binding Model.Password}" />
            <Entry Text="{Binding Model.ConfirmPassword}" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>